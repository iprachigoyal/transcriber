# LiveKit agent framework + RTC + server API
livekit-agents>=1.0.0,<2.0.0
livekit-api>=1.0.0

# Voice activity detection (webrtcvad imports pkg_resources at runtime, which
# ships with setuptools; modern venvs no longer include it by default).
webrtcvad==2.0.10
setuptools>=68,<81  # 81+ removed pkg_resources, which webrtcvad imports

# Audio buffering / resampling / WAV encoding
numpy>=1.26

# HTTP client for the Whisper Space
httpx>=0.27

# .env loading
python-dotenv>=1.0
